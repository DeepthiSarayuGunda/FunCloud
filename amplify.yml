version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Injecting environment variables..."
        - sed -i "s|%%API_BASE_URL%%|$API_BASE_URL|g" frontend/env-config.js
        - echo "Build complete"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
