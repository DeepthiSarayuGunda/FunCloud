version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd kids-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: kids-app/dist
    files:
      - '/**/*'
  cache:
    paths:
      - kids-app/node_modules/**/*
